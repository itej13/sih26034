# Key names only. Never commit values — this file is read by people, not by the app.

# Hosted vision-language model used for field extraction (stage 3).
# Provider not yet chosen; see Open Questions in the Projects wiki.
VISION_MODEL_API_KEY=

# Supabase — Dhruv's lane. Anon key is public by design; the service role key is not
# and must never appear in a client component.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
