## What this does

<!-- One or two sentences. What changed and why — not which files you touched. -->

## Lane

<!-- Dhruv (backend/data/evidence) · Shaurya (frontend/dashboard) · Advik (rule packs) -->

## Checks

- [ ] `npm run check` is green
- [ ] I did not change `lib/types.ts`, `fixtures/*` or `packs/*` — or I raised it at standup first
- [ ] This is one task, not several

## Anything you are unsure about

<!-- Say it here. "I put the hash on the evidence row rather than the scan row — say if that's
     wrong" gets an answer in a minute and saves a rewrite. Depends on another open PR? Say
     "depends on #N". -->
