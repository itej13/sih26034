import type { Finding } from "@/lib/types";
import { VerdictBadge } from "./VerdictBadge";
export function FindingCard({ finding }: { finding: Finding }) { return <article className="panel p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="eyebrow">Rule {finding.rule_ref}</p><h3 className="mt-1 font-semibold text-slate-950">Legal finding</h3></div><VerdictBadge verdict={finding.verdict} /></div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-slate-500">Measured</dt><dd className="mt-1 font-medium text-slate-900">{finding.measured}</dd></div><div><dt className="text-slate-500">Required</dt><dd className="mt-1 font-medium text-slate-900">{finding.required}</dd></div></dl>{finding.message && <p className="mt-4 text-sm text-slate-700">{finding.message}</p>}<blockquote className="mt-4 border-l-2 border-blue-800 pl-3 text-sm leading-6 text-slate-700">{finding.rule_text}</blockquote><p className="mt-3 text-xs font-medium text-slate-500">Rule pack: {finding.rule_pack}</p></article>; }
