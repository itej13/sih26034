import type { LabelField } from "@/lib/types";
export function FieldList({ fields }: { fields: LabelField[] }) { return <section className="panel p-5"><div><p className="eyebrow">Extraction output</p><h2 className="mt-1 text-xl font-semibold text-slate-950">Extracted declarations</h2></div><dl className="mt-5 divide-y divide-slate-100">{fields.map((field) => <div key={field.key} className="py-3 first:pt-0"><dt className="text-xs font-bold uppercase tracking-wide text-slate-500">{field.key.replace("_", " ")} · {(field.confidence * 100).toFixed(0)}% confidence</dt><dd className="mt-1 text-sm text-slate-800">{field.text}</dd></div>)}</dl></section>; }
